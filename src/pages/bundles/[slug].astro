---
// Bundles concept removed — redirect to homepage
export function getStaticPaths() {
  return [
    { params: { slug: 'openclaw-essentials' } },
    { params: { slug: 'connected-workspace' } },
    { params: { slug: 'starter' } },
    { params: { slug: 'quality' } },
  ];
}
return Astro.redirect('/', 301);
---
